name: dial_picker
description: A simple widget to pick Duration values. Forked with updated dependencies from https://github.com/juliansteenbakker/duration_picker.

version: 1.0.1+1

repository: https://github.com/rilpires/dial_picker

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter


  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_lints: ^2.0.0

  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
